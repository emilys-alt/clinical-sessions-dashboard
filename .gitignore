# Full transcripts stay local-only — never push these to GitHub.
# The public repo should only ever see episode_index.json (titles, URLs,
# and embedding vectors — see export_episode_index.py).
episodes/

# Local Python environment
venv/

# Local caches / generated files (regenerated by every run — no need to
# version-control these; the workflow regenerates docs/index.html fresh
# each time it runs in the cloud)
episode_embeddings.pkl
matches.json
dashboard_report.html

# macOS
.DS_Store
